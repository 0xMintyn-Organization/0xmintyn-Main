import express from 'express';

const router = express.Router();

// Placeholder routes - will be implemented in Phase 5
router.get('/', (req, res) => {
  res.json({ message: 'Order routes - Coming soon in Phase 5' });
});

export default router;
