import express from 'express';

const router = express.Router();

// Placeholder routes - will be implemented in Phase 4
router.get('/', (req, res) => {
  res.json({ message: 'Service routes - Coming soon in Phase 4' });
});

export default router;
