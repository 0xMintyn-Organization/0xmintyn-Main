import express from 'express';

const router = express.Router();

// Placeholder routes - will be implemented in Phase 2
router.get('/:slug', (req, res) => {
  res.json({ message: 'Shop routes - Coming soon in Phase 2' });
});

export default router;
