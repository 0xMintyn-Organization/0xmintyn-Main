import express from 'express';

const router = express.Router();

// Placeholder routes - will be implemented in Phase 6
router.get('/', (req, res) => {
  res.json({ message: 'Review routes - Coming soon in Phase 6' });
});

export default router;
