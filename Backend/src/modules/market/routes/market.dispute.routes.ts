import express from 'express';

const router = express.Router();

// Placeholder routes - will be implemented in Phase 7
router.get('/', (req, res) => {
  res.json({ message: 'Dispute routes - Coming soon in Phase 7' });
});

export default router;
